/*//Esto define la clase
    class Rectangulo {                              // nombre en con mayuscula, en una función nunca con mayuscula
        constructor(alto, ancho) {                  // constructor, sin funcion ni nada, solo los parametros si los tiene. esos parametros serás sus atributos
        this.alto = alto;                       //"this" guarda la direccion de memoria del objeto actual this.alto repreenta al atributo del propio objeto
        this.ancho = ancho;
        }
    }

    // Esto usa la clase
    var r = new Rectangulo(3,4);  //new pide memoria, la utiliza para crear. En este caso nos devuelve y guarda en "r" la dirección
    //alert(r.alto+" "+r.ancho);*/

       // Anonima
/*let Rectangulo = class Rectangulo2 {
 constructor(alto, ancho) {
     this.alto = alto;
     this.ancho = ancho;
 }
 };
 
 console.log(Rectangulo.name);
 // output: "Rectangulo"*/

 